{
  "name": "mood-tracker-server",
  "version": "1.0.0",
  "description": "Mood Pie Chart server",
  "main": "server.js",
  "scripts": {
    "start": "env NODE_ENV=\"production\" bash -c \"node server.js\"",
    "dev": "nodemon -e js --watch . --watch schemas server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Fabian Lozano",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "logger": "0.0.1",
    "mongoose": "^8.0.1"
  },
  "devDependencies": {
    "morgan": "^1.10.0",
    "nodemon": "^3.0.1"
  }
}
